import { StoryBlokUrlPipe } from './story-blok-url.pipe';

describe('StoryBlokUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new StoryBlokUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
